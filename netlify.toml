[build]
  functions = "functions/"

# [[redirects]]
#   from = "/api/*"
#   to = "/.netlify/functions/:splat"
#   status = 200

# [context.production.environment]
#   TOKEN = "production"

# [context.deploy-preview.environment]
#   TOKEN = "deploy-preview"
